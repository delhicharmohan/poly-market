# API Configuration
[[[[[[[[[[[[MERCHANT_API_KEY=your_merchant_api_key_here
API_BASE_URL=http://localhost:3001/v1
]]]]]]]]]]]]
# Database Configuration
DATABASE_URL=postgresql://indimarket:indimarket123@localhost:5434/indimarket
POSTGRES_PORT=5434

# Email (Resend) – for sending invoice + painting on purchase
RESEND_API_KEY=re_xxxxxxxxxxxx
RESEND_FROM_EMAIL=Merchandise <onboarding@resend.dev>

# Admin – sales and admin dashboard visible only to these users
# Option 1: comma-separated emails (must match users.email in DB)
ADMIN_EMAILS=admin@example.com
# Option 2: single Firebase UID (overrides ADMIN_EMAILS if set)
# ADMIN_FIREBASE_UID=your_firebase_uid_here

# Static admin login (separate from app login) – for /admin/login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_admin_password
# Optional: use a separate secret for signing admin session cookie (defaults to ADMIN_PASSWORD)
# ADMIN_SECRET=your_session_signing_secret
